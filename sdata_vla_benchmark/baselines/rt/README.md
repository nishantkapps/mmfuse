# RT-1 / RT-2 (frozen)

Public replicates or official releases vary by platform (JAX/TensorFlow). Use whichever checkpoint your lab can run **without** fine-tuning on SData.

Map policy outputs to the eight SData command indices for reporting. Document exact checkpoint name and preprocessing in the paper.

Stub: `run_rt.py`
